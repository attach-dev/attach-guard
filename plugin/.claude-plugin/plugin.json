{
  "name": "attach-guard",
  "version": "0.5.0",
  "description": "Supply-chain security guard that intercepts package install commands across npm, pnpm, pip, go, and cargo ecosystems and enforces package risk policies before execution.",
  "author": {
    "name": "Attach.dev"
  },
  "repository": "https://github.com/attach-dev/attach-guard",
  "license": "MIT",
  "keywords": ["security", "supply-chain", "npm", "pnpm", "hooks"],
  "skills": "./skills/",
  "userConfig": {
    "attach_open_score_api_token": {
      "title": "Attach Open Score API Token",
      "description": "Token for the hosted Open Score verdict API (score.attach.dev). Leave blank to score locally with the attach-open-score binary instead.",
      "type": "string",
      "sensitive": true
    }
  }
}
